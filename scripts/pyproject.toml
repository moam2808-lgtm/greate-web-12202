[project]
name = "supabase-migration"
version = "0.1.0"
description = "Migrate database to Supabase"
requires-python = ">=3.9"
dependencies = [
    "psycopg2-binary>=2.9.0",
    "python-dotenv>=0.19.0"
]
